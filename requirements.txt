requests>=2.31.0
plyer>=2.1.0           # 跨平台系统通知（macOS / Windows / Linux）
beautifulsoup4>=4.12.0
lxml>=5.0.0
browser-cookie3>=0.19.0
playwright>=1.40.0
python-dotenv>=1.0.0
rich>=13.7.1
platformdirs>=4.2.0
anthropic>=0.40.0   # 可选，用于自动识别验证码
mcp[cli]>=1.0.0     # MCP Server 支持
openai>=1.0.0       # Agent 聊天接口（兼容 DeepSeek 等 OpenAI 兼容 API）
pypdf>=4.0.0        # 读取作业 PDF 内容
browser-use>=0.12.0 # my.sjtu.edu.cn 自动浏览
langchain-openai>=0.3.0  # browser-use 使用的 LLM 接口
pytelegrambotapi>=4.0
lark-oapi>=1.4.0       # 飞书（Lark）官方 SDK
python-docx>=1.0.0     # Word 文档读写（作业分析）
fpdf2>=2.7.0           # PDF 生成（作业解答输出）
httpx>=0.26.0          # 微信 ilink bot 协议 HTTP 客户端
qrcode[pil]>=7.4       # 终端二维码生成（微信扫码登录）
